<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!-- Pinned here rather than inherited from .env, for two reasons. It stops
             the suite behaving differently on one developer's machine than on
             another's or on CI. And with debug ON, any test that provokes an
             unhandled exception renders the verbose stack-trace page, which reads
             the source of every frame (dozens of vendor files), so on a
             bind-mounted checkout one such test cost ~40s, a fifth of the whole
             run. What those tests assert is the status code, never the page. -->
        <env name="APP_DEBUG" value="false"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Tests MUST run on SQLite :memory:, never the dev MySQL, or
             RefreshDatabase migrate:fresh would wipe live data. force="true" is
             only the first layer: a real DB_* env var in the process still wins
             over it, so DB config is kept OUT of docker-compose/CI and lives in
             .env instead. The hard guarantee is Tests\TestCase, which aborts
             every test if the resolved connection is not sqlite :memory:. -->
        <env name="DB_CONNECTION" value="sqlite" force="true"/>
        <env name="DB_DATABASE" value=":memory:" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
