# API origin the SPA talks to. Copy this file to `.env` (uncommitted) and adjust per
# environment. The feed client reads VITE_API_BASE_URL via import.meta.env.
VITE_API_BASE_URL=http://localhost:8000
